/**
 * Config package
 *
 * Purpose: Application and framework configuration classes (beans, security placeholders, datasource, feign, AWS clients). Keeps configuration centralized and testable.
 */
package com.databridge.configservice.config;
